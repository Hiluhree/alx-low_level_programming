#include <stdio.h>
#include "main.h"

/**
 * _isupper - a function that checks for uppercase character.
 * @c: Character to be checked
 * Return: Returns 0
 */
int _isupper(int c)
{
	return (c >= 'A' && c <= 'Z');
}
