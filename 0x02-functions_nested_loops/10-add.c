#include "main.h"

/**
 * add - a function that adds two integers and returns the result.
 * @x: first value
 * @y: second value
 * Return: returns the result for addition
 */
int add(int x, int y)
{
	int z;

	z = x + y;
	return (z);
}
